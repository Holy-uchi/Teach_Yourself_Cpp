int main() {}

// この設計の問題点は、共有カウンタを更新するたびに mutex をロックするため、更新頻度が高いとロック競合が発生し、性能低下の原因になることである。特に単純な加算処理では、計算そのものより同期コストの方が大きくなりやすい。

// 改善案として、共有しているのが単なるカウンタであれば std::atomic を用いてロックなしで安全に更新できる。また、更新回数が非常に多い場合は、各スレッドでローカルに加算してから最後にまとめて共有カウンタへ反映する設計にすると、競合をさらに減らせる。

// したがって、必ずしも mutex を使い続ける必要はない。単純な単一カウンタ更新なら atomic が適し、より高頻度な更新ではスレッドローカル集計などの設計変更も有効である。一方、複数の共有データをまとめて整合性付きで更新する場合は mutex が必要になる。